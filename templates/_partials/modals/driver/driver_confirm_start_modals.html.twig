<div class="modal fade" id="startModal-{{ carpool.id }}" tabindex="-1" aria-labelledby="startModalLabel-{{ carpool.id }}" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="cancelModalLabel">Débuter le covoiturage-{{ carpool.carpoolNumber }}</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<div class="modal-body">
				<p><strong>Êtes-vous sûr de vouloir commencer votre covoiturage</strong>?</p>

					<p><strong>Date :</strong>{{ carpool.day|date('d/m/Y') }}</p>
                    <p><strong>Heure de départ :</strong>{{ carpool.begin|date('H:i') }}</p>

				<p><strong>Nom voyageur(s) :</strong>
					{% for user in carpool.user %}
						<span>{{ user.pseudo }}</span>
					{% else %}
						<span>Aucune réservation</span>
					{% endfor %}
				</p>
			</p>
		</div>
		<div class="d-flex justify-content-between modal-footer">
			<form method="POST" action="{{ path('app_driver_carpool_start', { id: carpool.id }) }}">
				<button type="submit" class="btn btn-info">Début</button>
			</form>
		</div>
	</div>
</div></div>
